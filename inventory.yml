all:
  children:
    webservers:
      hosts:
        web1.dip.lom:
        web2.dip.lom:

    monitoring:
      hosts:
        grafana.dip.lom:
        prometheus.dip.lom:

    elk:
      hosts:
        es.dip.lom:
        kibana.dip.lom:

    logs:
      hosts:
        es:
          ansible_host: 51.250.6.152
        kibana:
          ansible_host: 130.193.50.37

    filebeat_servers:
      webservers:

    docker_servers:
      logs:
      #filebeat_servers:
  
  vars:
    ansible_user: serv_acc