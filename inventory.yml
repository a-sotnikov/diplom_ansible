all:
  children:
    webservers:
      hosts:
        web1.dip.lom:
        web2.dip.lom:

    monitoring:
      children:
        grafana:
          hosts:
            grafana.dip.lom:
        prometheus:
          hosts:
            prometheus.dip.lom:

    logging:
      children:
        elasticsearch:
          hosts:
            es.dip.lom:
        kibana:
          hosts:
            kibana.dip.lom:

    filebeat_servers:
      webservers:

    docker_servers:
      logging:
      filebeat_servers:
  
    ansible_user: serv_acc