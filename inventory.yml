all:
  children:
    webservers:
      hosts:
        web1.dip.lom:
        web2.dip.lom:

    monitoring:
      children:
        grafana:
          hosts:
            grafana.dip.lom:
        prometheus:
          hosts:
            prometheus.dip.lom:

    logging:
      children:
        elasticsearch:
          hosts:
            es.dip.lom:
        kibana:
          hosts:
            kibana.dip.lom:

    filebeat_servers:
      children:
        webservers:

    fluent-bit_servers:
      children:
        webservers:

    docker_servers:
      children:
        logging:
        filebeat_servers:

  vars:
    ansible_user: serv_acc
