all:
  children:
    webservers:
      hosts:
        web1.dip.lom:
        web2.dip.lom:

    monitoring:
      hosts:
        grafana.dip.lom:
        prometheus.dip.lom:

    elk:
      hosts:
        es.dip.lom:
        kibana.dip.lom:

    logs:
      hosts:
        es:
          ansible_host: 84.201.158.100
        kibana:
          ansible_host: 51.250.64.34

    filebeat_servers:
      webservers:
  
  vars:
    ansible_user: serv_acc